{
  "name": "nuxt-gen",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve",
    "heroku-prostbuild": "npm run build",
    "generate": "nuxt build --target static && nuxt export"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.0",
    "http-server": "^0.12.3",
    "json-server": "^0.16.1",
    "nuxt": "^2.13.0"
  },
  "devDependencies": {}
}
